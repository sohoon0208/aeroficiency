export const CANONICAL_AGENT_TASK = 'Inspect the current editable Baseline reference. Create a candidate that reduces modeled wing-box wall mass by at least 5% while maintaining all five configured trade-study checks and leaving the wake-induced-drag estimate no worse at the same target lift. Keep the selected Baseline fixed during this comparison unless the user explicitly asks to change it. Analyze each proposal and correct the candidate if the objective is not met. Inspect the final candidate’s root station, then compare the current immutable Baseline and candidate analyses. Treat numerically tiny drag changes as no meaningful improvement.';

export const CANONICAL_TRADE_OFF_SENTENCE = 'The candidate achieves a meaningful modeled wing-box wall-mass reduction while no meaningful improvement is claimed in the wake-induced-drag estimate.';
